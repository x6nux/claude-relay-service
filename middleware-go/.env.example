# 环境变量配置示例

# 服务配置
PORT=8080
GIN_MODE=production

# Redis配置 (与主服务保持一致)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=""
REDIS_DB=0

# 代理配置
TARGET_URL=http://localhost:3001  # Node.js主服务地址
PROXY_TIMEOUT=300                  # 代理超时时间(秒)